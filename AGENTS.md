# 이음 스튜디오 프로젝트 지침

## 제품 원칙

- 표시명은 `이음 스튜디오`다.
- 원고가 화면과 시스템의 주인공이다.
- 별빛·복선·음악·조수·투고 운영은 독립 원본 또는 보조 기능이다.
- 외부 연결을 모두 끊어도 집필·복구·기록·백업의 핵심 폐회로가 동작해야 한다.
- 사용자가 선택한 범위, 작품 소유권, 제출 당시 버전, 활동 기록을 임의로 확장·대체·추정하지 않는다.

## 시작 전 읽기

변경 전에 반드시 다음을 읽는다.

1. `README.md`
2. `plan.md`
3. `docs/design-baseline.md`
4. 현재 단계가 참조하는 승인 설계 원문

설계 원문의 checksum이 manifest와 다르면 변경 내용을 검토하기 전 구현을 계속하지 않는다.

## 레거시 경계

- `D:\eum.editor`는 읽기 전용 이주 원본이다.
- 명시적 이주 도구 작업 전에는 레거시 파일·DB·브라우저 저장소를 쓰지 않는다.
- 레거시 소스 파일을 새 프로젝트로 통째 복사하지 않는다.
- 레거시 타입명·화면 구조·제품명·캐릭터명·샘플 데이터를 새 정규 도메인의 기본값으로 채택하지 않는다.
- 이주 실험은 원본 snapshot을 봉인한 뒤 별도 임시 저장소에서만 실행한다.

## 아키텍처 경계

- domain은 순수 TypeScript이며 Electron·React·DB·파일 API를 import하지 않는다.
- application은 명령·조회·트랜잭션 경계를 소유한다.
- platform은 SQLite·blob·파일·비밀 저장·OAuth·clock adapter를 소유한다.
- Electron main은 저장·비밀·앱 생명주기를 조정한다.
- preload는 좁은 typed bridge만 노출한다.
- renderer는 DB·파일·OAuth token에 직접 접근하지 않는다.
- Electron 전용 import는 platform/desktop 경계 밖으로 퍼뜨리지 않는다.

## 데이터 불변식

- 작품 밖 데이터의 암묵적 fallback을 금지한다.
- 원고 변경은 명시적 편집 명령으로만 수행한다.
- 사용자의 선택 범위를 임의로 행·문단 단위로 확장하지 않는다.
- 근거 없는 위치·상태·관계를 자동 확정하지 않는다.
- 파생 뷰를 독립 원본으로 중복 저장하지 않는다.
- 조수·메일·외부 가져오기 결과는 승인 전 Candidate다.
- DocumentRevision·WorkSnapshot·SubmissionPackage는 생성 후 불변이다.
- 실제 활동 시간은 WritingSession 원장 하나에서 파생한다.
- 미매핑 외부 필드와 고아 데이터는 삭제하지 않고 원시 보존한다.

## 하드코딩 금지

사용자가 정확한 예외를 요청하지 않는 한 다음을 제품 코드·테스트·설정·prompt에 고정하지 않는다.

- 작품·문서·인물·별빛·복선 ID와 이름
- 제공자·모델·endpoint·client ID
- callback host·port·Redirect URI
- 사용자 데이터 경로
- 분류 표시값
- 원고 길이 최대값
- 성능 시험 수치를 제품 제한으로 쓰는 동작

런타임 입력, schema, 사용자 사전, connector manifest, fixture manifest로 해결한다.

## 제공자와 조수

- 특정 AI 제공자나 모델을 대표·기본·주인공으로 취급하지 않는다.
- 조수는 원고를 대신 쓰지 않는다.
- 어휘·유의어·검색·구조 점검 등 승인된 능력만 권한 계약에 따라 실행한다.
- 문맥 접근은 사전 승인 또는 요청 시 승인으로 제한하고 receipt를 남긴다.
- token·API key·OAuth secret은 renderer·로그·localStorage에 두지 않는다.

## UI 조사

- 사용자가 명시적으로 요청하지 않는 한 Lazyweb을 사용하지 않는다.
- 승인된 화면 흐름과 실제 제품 요구를 우선한다.
- UI 변경은 실제 패키징된 앱 또는 승인된 POC 런타임에서 검증한다.

## 개발 절차

- 기존 계획을 실행할 때 한 Gate씩 진행한다.
- 새 dependency와 런타임 버전은 공식 현재 지원 범위를 확인하고 lockfile에 고정한다.
- 기능·버그 수정은 실패 검증부터 작성한다.
- 측정 fixture는 제품 제한이나 사용자 데이터 기본값으로 사용하지 않는다.
- unrelated 파일을 정리하거나 되돌리지 않는다.
- 커밋·푸시는 사용자가 요청했을 때만 한다.

## 완료 증거

완료 주장은 해당 단계의 최신 검증 결과가 있어야 한다.

- 타입·정적 검사
- 단위·통합 검증
- 패키징된 데스크톱 E2E
- 성능 측정 JSON
- 강제 종료·복구 결과
- 이주 source·target checksum

현재 검증 명령은 scaffold 후 `plan.md`와 프로젝트 README에 기록한다. 명령이 아직 없을 때 임의의 통과 결과를 주장하지 않는다.
