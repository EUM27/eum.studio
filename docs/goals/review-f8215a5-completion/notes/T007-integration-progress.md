# Integration failures and actual-path fixes

The second core runner (`2026-09-04T23-52-57-539Z-63aa34c8-1aa1-4b18-aa52-75628bad6003`) ran lint, four TypeScript projects, 351 Vitest files / 1292 passing tests, architecture over 651 production TS/JS files, and production build with exit 0. Its initial/final code fingerprints were identical (`1c189238899dfaa0de5d3321121401578bb7efa9dbc2c05be913ed366f9c7961`), but a filename-less native watch notification prevented the Electron stage. The report is retained as failed, not reclassified as passed. The Worker is correcting source observation under generated-file activity with directory-scoped watching and explicit reconciliation.

Root ran the existing exact eight core Electron cases directly. Seven passed in that invocation. The first-Work case stopped on an obsolete selector which matched both an event's open and detach controls. Inspection of `BottomEventRail.tsx` confirmed the independent controls. Both pre/restart locators in `manuscript-activity.spec.ts` now target the actual `.bottom-event-card-open` element with the fixture's event title; no assertion was removed and no `.first()`/forced click was added.

The next run reached a real pointer-interception failure at `장면 추가`. An isolated Electron probe and screenshot showed the default floating compact session panel over the review rail. `document-rail-compat.css` now reuses the existing static status-row styling while the review rail is open and the panel is collapsed/unmoved. Explicit user-moved positions and expanded panels keep their behavior. No new layout dimensions were introduced. The probe's elementFromPoint changed from the focus button to the intended create-scene button. The current screenshot/geometry are `artifacts/review-f8215a5/ui-layout-probe.png` and `.json`; the original failure and source are in the retained first-work trace/error context.

Fresh `npx playwright test --config playwright.core.config.ts --project first-work-save-reopen --output artifacts/review-f8215a5/first-work-reopen-layout-fixed` passed (1 test, 1.5 minutes, exit 0). It includes actual Work creation, typing, named snapshot and revision restore, event/scene creation, focus/session state, full app restart and exact source selection round trips. `npm run build:renderer` and targeted ESLint also returned exit 0. This targeted run does not replace the final bundled core/heavy runs.

The ui-regression-pass skill's old `src/ui` file paths are not part of this repository. The equivalent current validation uses the four configured TypeScript projects, actual current unit suites and production/packaged Electron workflows recorded in the final receipt.
