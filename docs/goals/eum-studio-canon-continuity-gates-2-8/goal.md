# 작품 별빛·연속성 Gate 2–8 완결

## Objective

완료된 Gate 0·1 별빛 변경 검토 기반 위에 지정 설계의 Gate 2부터 최종 Gate 8까지를 현재 `D:\eum.studio` 도메인·SQLite·typed bridge·React 작업면 계약에 맞춰 순차 구현하고, 실제 production Electron과 재실행·강제 종료·성능·독립 구현 증거로 전체 설계를 완결한다.

## Original Request

`최종단계까지 계속 진행해`

## Intake Summary

- Input shape: `existing_plan`
- Audience: 이음 스튜디오 장편 집필 사용자
- Authority: `approved`
- Proof type: `test`, `demo`, `artifact`, `metric`, `review`
- Completion proof: Gate 2–8의 각 정규 원장·명령·조회·UI·migration이 production path에 연결되고, Work/revision/Anchor/Candidate/permission 불변식과 offline 핵심 폐회로를 지키며, fresh 정적·단위·통합·Electron·process-kill·성능·export 검증 및 최종 Judge 감사가 모두 현재 소스에서 재현된다.
- Likely misfire: 기존 Plot/Foreshadow/Character/Lore/WorkSnapshot을 새 이름의 중복 원장으로 복제하거나, 문서·fixture·성공 문자열만 추가하고 실제 typed runtime·원자 transaction·재실행 UI를 구현하지 않는 것.
- Blind spots considered: 185개 precise dirty entry 보존, schema 18 이후 migration 연쇄, 기존 full Vitest의 비관련 RuntimeBootstrapController AST 실패, stable sceneId/lineage의 실제 현재 계약, AI credential 없이도 검증 가능한 결정론적 connector fixture, renderer commit·대량 manifest·digest stale 측정, 단방향 export와 양방향 import의 엄격한 경계.

## Current Tranche

지정 설계의 Gate 2–8 전체가 현재 Goal의 범위다. 한 번에 누적하지 않고 다음 순서를 지킨다.

1. Gate 2 — 열린 연속성
2. Gate 3 — 인물 지식·믿음
3. Gate 4 — Context Planner와 활동
4. Gate 5 — 이야기 지금까지
5. Gate 6 — 안정적인 Scene identity·lineage 통합
6. Gate 7 — 명명된 WorkSnapshot 기준점과 장면 단위 비교 UX
7. Gate 8 — 별빛의 Obsidian 호환 단방향 Markdown export

각 Gate는 현재 소스 지도화·계약/원장·runtime/bridge·renderer·fresh 증거·Judge 감사를 마친 뒤 다음 Gate로 넘어간다.

## Non-Negotiable Constraints

- 정확한 승인 설계는 `C:\Users\limoj\OneDrive\바탕 화면\eum-studio-canon-continuity-independent-design.md`, SHA-256 `343577C44367C4E38A61913822A13400B5DAA028BE2EA76123F7BBF6A31F3FC5`다.
- 기존 Character·CharacterRelation·LoreEntry·PlotThread·ForeshadowLine·EventBlock·SceneIdentity·WorkSnapshot을 중복 별빛으로 복제하지 않는다.
- domain/application/platform/desktop/preload/renderer 경계를 유지하며 renderer는 DB·파일·token·model parser를 소유하지 않는다.
- AI 결과는 승인 전 Candidate이고, permission·ContextReceipt·ContextManifest를 우회하지 않는다.
- DocumentRevision·WorkSnapshot은 불변이며 원고 변경은 명시적 편집 명령으로만 수행한다.
- 제품 제한·사용자 기본값·고정 provider/model/path/port를 fixture 수치에서 만들지 않는다.
- 기존 dirty 파일은 사용자 소유로 보존하고 unrelated 변경을 되돌리거나 정리하지 않는다.
- 새 기능·버그 수정은 성공 기준 검증을 먼저 작성하고, known-failure 의식용 테스트는 만들거나 실행하지 않는다.
- 실제 사용자 DB·레거시 `D:\eum.editor`는 쓰지 않는다.
- 커밋·푸시는 사용자가 요청하지 않았으므로 하지 않는다.

## Stop Rule

Gate 2–8 전체 원래 결과를 최종 감사가 `full_outcome_complete: true`로 판정할 때만 멈춘다. 한 Gate나 한 slice의 통과는 다음 Gate로 이동하는 checkpoint일 뿐 완료가 아니다.

## Canonical Board

Machine truth lives at:

`docs/goals/eum-studio-canon-continuity-gates-2-8/state.yaml`

If this charter and `state.yaml` disagree, `state.yaml` wins for task status, active task, receipts, verification freshness, and completion truth.

## PM Loop

1. Read this charter and `state.yaml`.
2. Work only on the one active task.
3. Preserve the exact design checksum and start dirty fingerprint.
4. Record a compact receipt before selecting the next task.
5. Keep at most one write-capable task active.
6. Run a Judge audit after every Gate and continue immediately to the next safe Gate.
7. Finish only with a final audit receipt containing `full_outcome_complete: true`.

